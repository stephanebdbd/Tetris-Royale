#include "chatRoom.hpp"
#include <algorithm>



/*void chatRoom::addClient(int id) {
    std::lock_guard<std::mutex> lock(clientsMutex);
    
}

void chatRoom::removeClient(int id) {
    std::lock_guard<std::mutex> lock(clientsMutex);
    
}

void chatRoom::sendClientRequest(const std::string& pseudoName, const std::string& message) {
    std::lock_guard<std::mutex> lock(clientsMutex);
    
}

void chatRoom::acceptClientRequest(int id) {
    std::lock_guard<std::mutex> lock(clientsMutex);
}

void chatRoom::broadcastMessage(const std::string& message, const std::string& sender) {
    std::lock_guard<std::mutex> lock(clientsMutex);
}

std::string chatRoom::getChatMenu() const {
    return "Vous êtes dans le chat. Tapez votre message et appuyez sur Entrée.\n";
}*/